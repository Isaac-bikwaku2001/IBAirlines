@model IEnumerable<IBAirlines.Models.Avion>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Marque)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Type)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Capacite)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateMiseEnService)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Marque)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Capacite)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateMiseEnService)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AvionID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AvionID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AvionID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
